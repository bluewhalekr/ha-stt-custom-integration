# Service descriptions for the custom_stt_renew_rs integration.
# This integration implements the Home Assistant STT interface and does not provide additional services.
# The STT functionality is accessed through Home Assistant's built-in stt.* services.